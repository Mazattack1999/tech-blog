<h1>Your Dashboard</h1>

<section class="posts">
    {{#each posts as |post|}}
    <div> 
        {{> post-info post }}
    </div>
    {{/each}}
</section>

<button type="button" class="btn">+ New Post</button>
