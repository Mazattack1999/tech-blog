<h1>The Tech Blog</h1>

<section class="posts">
    {{#each posts as |post|}}
    <div> 
        {{> post-info post }}
    </div>
    {{/each}}
</section>

<section class="comments">
    
</section>

